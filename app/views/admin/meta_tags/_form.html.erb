<%= form_with(model: [:admin, meta_tag], local: true) do |form| %>
  <% if meta_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meta_tag.errors.count, "error") %> prohibited this meta tag from being saved:</h2>
      <ul>
        <% meta_tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :page %>
    <%= form.text_field :page %>
  </div>
  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %> 